#include <stdio.h>
#include <conio.h>
int main()
{
	int a=0,i,sum=0;
	for (i=0;i<20;i++)
	{   a=a+1;
		if(a%2==0)  
		{
			sum=sum+a;
		}
	}
	printf("sum of first 20 even numbers: %d",sum);
	getch();
}
